<div class="large-12 small-12 columns">
  <%= render 'cms/available_locales/side_nav' %>

  <div class="medium-7 small-12 columns">
    <h3>Choose a reference system:</h3>
    <ul>
      <% @reference_systems.order(updated_at: :asc).each do |reference_system| %>
        <li class="<%= 'needs-translation' if reference_system.needs_translation?(@available_locale.key) %>">
          <%= link_to reference_system.name, edit_cms_available_locale_reference_system_path(@available_locale, reference_system) %>
        </li>
      <% end %>
    </ul>
  </div>
  <div class="medium-3 small-12 columns">
    <div class="panel">
      <h6>What is it?</h6>
      <p>A Reference System represents a hypothetical
      solution which the company can provide for a
      given application. These provide customers with
      an idea of what the company offers in a given
      Vertical Market.</p>
    </div>
  </div>
</div>
