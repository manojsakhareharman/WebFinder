<div class="large-12 small-12 columns">
  <%= render 'cms/available_locales/side_nav' %>

  <div class="medium-7 small-12 columns">
    <h3><%= @available_locale.name %> Artists</h3>
    <%= link_to new_cms_available_locale_artist_path(@available_locale),
      class: "tiny success button radius" do %>
      <%= fa_icon "plus" %>
      Create New Artist
    <% end %>
    <% if @artists.count > 0 %>
      <table width="100%">
        <tr>
          <th>Name <i>(click to edit artist)</i></th>
          <th>Public Link <i>(click to view artist)</i></th>
        </tr>
        <% @artists.order(:name).each do |artist| %>
          <tr>
            <td><%= link_to artist.name, edit_cms_available_locale_artist_path(@available_locale, artist) %></td>
            <td>
              <%= link_to(artist_url(artist, locale: artist.locale.key),
                          artist_url(artist, locale: artist.locale.key),
                         target: "_blank") %>
            </td>
          </tr>
        <% end %>
      </table>
    <% end %>
  </div>
  <div class="medium-3 small-12 columns">
    <div class="panel">
      <h6>What is it?</h6>
      <p>An artist is typically a famous musician,
      producer, live production, etc. who uses
      a significant amount of HARMAN products
      in their work.</p>
      <p>
      <%= fa_icon "info-circle" %>
      Unlike other content types on this site, artists
      <u>only</u> appear on the site corresponding to
      the locale where they were created.</p>
    </div>
  </div>
</div>
