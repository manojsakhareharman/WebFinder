<div class="large-12 small-12 columns">
  <%= render 'cms/available_locales/side_nav' %>

  <div class="medium-7 small-12 columns">
    <h3>Choose a site setting:</h3>
    <ul>
      <% @site_settings.order(updated_at: :asc).each do |site_setting| %>
        <li class="<%= 'needs-translation' if site_setting.needs_translation?(@available_locale.key) %>">
          <%= link_to site_setting.name, edit_cms_available_locale_site_setting_path(@available_locale, site_setting) %>
        </li>
      <% end %>
    </ul>
  </div>
  <div class="medium-3 small-12 columns">
    <div class="panel">
      <h6>What is it?</h6>
      <p>Site Settings are used throughout the
      site in many places. The names of each
      should provide a clue as to their purpose.</p>
    </div>
  </div>
</div>
