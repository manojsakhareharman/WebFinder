<div class="large-12 small-12 columns">
  <%= render 'cms/available_locales/side_nav' %>

  <div class="medium-7 small-12 columns">
    <h3>Choose a case study:</h3>
    <ul>
      <% @case_studies.order('headline').each do |case_study| %>
        <li class="<%= 'needs-translation' if case_study.needs_translation?(@available_locale.key) %>">
          <%= link_to case_study.headline, edit_cms_available_locale_case_study_path(@available_locale, case_study) %>
        </li>
      <% end %>
    </ul>
  </div>
  <div class="medium-3 small-12 columns">
    <div class="panel">
      <h6>What is it?</h6>
      <p>A Case Study represents an actual
      installation/application of the company's
      products. It relates to the Vertical
      Market and is linked to from Reference
      System pages.</p>
    </div>
  </div>
</div>
