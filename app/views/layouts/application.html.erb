<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) + " | " : '' %>Harman Professional</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div class="sticky">
      <nav class="top-bar" data-topbar role="navigation">
        <ul class="title-area">
          <li class="name">
          <h1><a href="/"><%= image_tag "harman-logo.png", alt: "Harman Professional Audio, Video, Lighting and Control Systems" %></a></h1>
          </li>
          <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
        </ul>

        <section class="top-bar-section">
          <ul class="right">
            <%= top_vertical_market_navigation %>
          </ul>
        </section>
      </nav>
    </div>

    <% if content_for?(:banner) %>
      <div class="bumped-up space-after">
        <%= yield :banner %>
      </div>
    <% end %>

    <div class="container main-container">
      <div class="row">
        <div class="large-8 large-offset-2 columns">
          <%= yield %>
        </div>
      </div>
    </div>

    <%= cache ["footer", Date.today] do %>
      <div class="container footer">
        <div class="row">
          <div class="large-8 large-offset-2 columns">
            <div class="row">
              <div class="large-3 columns">
                <% VerticalMarket.parent_verticals.each do |vmp| %>
                  <% if vmp.children.length > 0 %>
                    <h6 class="subheader"><%= vmp.name %></h6>
                    <ul>
                      <% vmp.children.each do |v| %>
                        <li><%= link_to v.name, v %></li>
                      <% end %>
                    </ul>
                  <% end %>
                <% end %>
              </div>
              <div class="large-3 columns">
                <% VerticalMarket.parent_verticals.each do |vmp| %>
                  <% if vmp.children.length <= 0 %>
                    <h6 class="subheader"><%= vmp.name %></h6>
                    <ul>
                      <% vmp.reference_systems.each do |rs| %>
                        <li><%= link_to rs.name, [vmp, rs] %></li>
                      <% end %>
                    </ul>
                  <% end %>
                <% end %>
              </div>
              <div class="large-3 columns">
                <h6 class="subheader">Harman Brands</h6>
                <ul>
                  <% Brand.order("UPPER(name)").each do |brand| %>
                    <li><%= link_to brand.name, brand.url, target: "_blank" %></li>
                  <% end %>
                </ul>
              </div>
              <div class="large-3 columns">
                <h6 class="subheader">Harman Resource Library</h6>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <%= javascript_include_tag "application" %>
  </body>
</html>
